// TODO(adriana): survey form, gamified appliance-photo upload, loop/menu screen.
// Build against SurveyInputSchema in @/lib/schemas/survey.
export default function SurveyPage() {
  return <div>survey — TODO(adriana)</div>;
}
